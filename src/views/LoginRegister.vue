<template>
  <div class="login-container">
    <!--=============== LOGIN IMAGE ===============-->
    <svg class="login__blob" viewBox="0 0 566 840" xmlns="http://www.w3.org/2000/svg">
      <mask id="mask0" mask-type="alpha">
        <path d="M342.407 73.6315C388.53 56.4007 394.378 17.3643 391.538 
        0H566V840H0C14.5385 834.991 100.266 804.436 77.2046 707.263C49.6393 
        591.11 115.306 518.927 176.468 488.873C363.385 397.026 156.98 302.824 
        167.945 179.32C173.46 117.209 284.755 95.1699 342.407 73.6315Z"/>
      </mask>

      <g mask="url(#mask0)">
        <path d="M342.407 73.6315C388.53 56.4007 394.378 17.3643 391.538 
        0H566V840H0C14.5385 834.991 100.266 804.436 77.2046 707.263C49.6393 
        591.11 115.306 518.927 176.468 488.873C363.385 397.026 156.98 302.824 
        167.945 179.32C173.46 117.209 284.755 95.1699 342.407 73.6315Z"/>

        <image class="login__img" href="../assets/img/bg-img.jpg"/>
      </g>
    </svg>

    <!--=============== LOGIN ===============-->
    <div class="login container grid" id="loginAccessRegister">
      <!--===== LOGIN ACCESS =====-->
      <div class="login__access" v-if="isLoginActive">
        <h1 class="login__title">登录你的账户</h1>

        <div class="login__area">
          <form @submit.prevent="login" class="login__form">
            <div class="login__content grid">
              <div class="login__box">
                <input type="email" id="email" required placeholder=" " class="login__input" v-model="email">
                <label for="email" class="login__label">邮箱</label>
                <i class="ri-mail-fill login__icon"></i>
              </div>

              <div class="login__box">
                <input 
                  :type="showPassword ? 'text' : 'password'" 
                  id="password" 
                  required 
                  placeholder=" " 
                  class="login__input" 
                  v-model="password"
                />
                <label for="password" class="login__label">密码</label>
                <i 
                  :class="showPassword ? 'ri-eye-fill' : 'ri-eye-off-fill'" 
                  class="login__icon login__password" 
                  @click="togglePassword"
                ></i>
              </div>
            </div>

            <a href="#" class="login__forgot">忘记密码?</a>

            <button type="submit" class="login__button">登录</button>
          </form>

          <div class="login__social">
            <p class="login__social-title">其他方式</p>

            <div class="login__social-links">
              <a href="#" class="login__social-link">
                <img src="../assets/img/icon-google.svg" alt="image" class="login__social-img">
              </a>

              <a href="#" class="login__social-link">
                <img src="../assets/img/icon-facebook.svg" alt="image" class="login__social-img">
              </a>

              <a href="#" class="login__social-link">
                <img src="../assets/img/icon-apple.svg" alt="image" class="login__social-img">
              </a>
            </div>
          </div>

          <p class="login__switch">
            没有账户? 
            <button @click="toggleLoginRegister">注册账户</button>
          </p>
        </div>
      </div>

      <!--===== LOGIN REGISTER =====-->
      <div class="login__register" v-else>
        <h1 class="login__title">Create new account.</h1>

        <div class="login__area">
          <form @submit.prevent="register" class="login__form">
            <div class="login__content grid">
              <div class="login__group grid">
                <div class="login__box">
                  <input type="text" id="names" required placeholder=" " class="login__input" v-model="names">
                  <label for="names" class="login__label">Names</label>
                  <i class="ri-id-card-fill login__icon"></i>
                </div>

                <div class="login__box">
                  <input type="text" id="surnames" required placeholder=" " class="login__input" v-model="surnames">
                  <label for="surnames" class="login__label">Surnames</label>
                  <i class="ri-id-card-fill login__icon"></i>
                </div>
              </div>

              <div class="login__box">
                <input type="email" id="emailCreate" required placeholder=" " class="login__input" v-model="emailCreate">
                <label for="emailCreate" class="login__label">Email</label>
                <i class="ri-mail-fill login__icon"></i>
              </div>

              <div class="login__box">
                <input 
                  :type="showRegisterPassword ? 'text' : 'password'" 
                  id="passwordCreate" 
                  required 
                  placeholder=" " 
                  class="login__input" 
                  v-model="passwordCreate"
                />
                <label for="passwordCreate" class="login__label">Password</label>
                <i 
                  :class="showRegisterPassword ? 'ri-eye-fill' : 'ri-eye-off-fill'" 
                  class="login__icon login__password" 
                  @click="toggleRegisterPassword"
                ></i>
              </div>
            </div>

            <button type="submit" class="login__button">Create account</button>
          </form>

          <p class="login__switch">
            Already have an account? 
            <button @click="toggleLoginRegister">Log In</button>
          </p>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue';

const isLoginActive = ref(true); // 是否处于登录界面
const showPassword = ref(false); // 登录密码是否显示
const showRegisterPassword = ref(false); // 注册密码是否显示

const email = ref('');
const password = ref('');
const names = ref('');
const surnames = ref('');
const emailCreate = ref('');
const passwordCreate = ref('');

// 切换登录和注册表单
const toggleLoginRegister = () => {
  isLoginActive.value = !isLoginActive.value;
  console.log('isLoginActive:', isLoginActive.value); // 调试切换状态
};

// 登录方法
const login = () => {
  console.log('Login with:', email.value, password.value);
};

// 注册方法
const register = () => {
  console.log('Register with:', names.value, surnames.value, emailCreate.value, passwordCreate.value);
};

// 切换密码显示状态
const togglePassword = () => {
  showPassword.value = !showPassword.value;
};

// 切换注册密码显示状态
const toggleRegisterPassword = () => {
  showRegisterPassword.value = !showRegisterPassword.value;
};
</script>


<style scoped>
/* Add the styles as needed based on your existing styles */
/*=============== GOOGLE FONTS ===============*/
@import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@100..900&display=swap");

/*=============== VARIABLES CSS ===============*/
/*========== Colors ==========*/
/*Color mode HSL(hue, saturation, lightness)*/
:root {
  /*========== Font and typography ==========*/
  /*.5rem = 8px | 1rem = 16px ...*/
  --body-font: "Montserrat", system-ui;
  --big-font-size: 1.5rem;
  --normal-font-size: .938rem;
  --small-font-size: .813rem;
  --tiny-font-size: .688rem;

  /*========== Font weight ==========*/
  --font-regular: 400;
  --font-medium: 500;
  --font-semi-bold: 600;

  /*========== z index ==========*/
  --z-tooltip: 10;
  --z-fixed: 100;
}

/*========== Responsive typography ==========*/
@media screen and (min-width: 1150px) {
  :root {
    --big-font-size: 3rem;
    --normal-font-size: 1rem;
    --small-font-size: .875rem;
    --tiny-font-size: .75rem;
  }
}

/*=============== BASE ===============*/
* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

body,
input,
button {
  font-family: var(--body-font);
  font-size: var(--normal-font-size);
}

body {
  background-color: #ffffff; /* White color */
  color: hsl(220, 15%, 66%); /* Text color */
}

input,
button {
  border: none;
  outline: none;
}

a {
  text-decoration: none;
}

img {
  display: block;
  max-width: 100%;
  height: auto;
}

/*=============== REUSABLE CSS CLASSES ===============*/
.container {
  max-width: 1120px;
  margin-inline: 1.5rem;
}

.grid {
  display: grid;
  gap: 1rem;
}

/*=============== LOGIN  ===============*/
.login {
  position: relative;
  height: 100vh;
  align-items: center;
  overflow: hidden;
}

.login__blob {
  display: none;
}

.login__title {
  font-size: 3rem; /* Big font size */
  color: hsl(220, 68%, 4%); /* Title color */
  text-align: center;
  margin-bottom: 2rem;
}

.login__box {
  position: relative;
  display: flex;
  align-items: center;
  background-color: hsl(219, 23%, 78%); /* Background color */
  border-radius: 1rem;
}

.login__input {
  background: none;
  width: 100%;
  padding: 1.5rem 2.5rem 1.5rem 1.25rem;
  font-weight: 600; /* Semi-bold font */
  border: 3px solid transparent;
  border-radius: 1rem;
  z-index: 1;
  transition: border-color .4s;
}

.login__input:autofill {
  transition: background-color 6000s, color 6000s;
}

.login__label {
  position: absolute;
  left: 1.25rem;
  font-weight: 600; /* Semi-bold font */
  transition: transform .4s, font-size .4s, color .4s;
}

.login__icon {
  position: absolute;
  right: 1rem;
  font-size: 1.25rem;
  transition: color .4s;
}

.login__password {
  cursor: pointer;
  z-index: 10;
}

.login__forgot {
  display: block;
  width: max-content;
  margin: 1rem 0 0 auto;
  font-size: .813rem; /* Small font size */
  font-weight: 600; /* Semi-bold font */
  color: hsl(220, 15%, 66%); /* Text color */
  transition: color .4s;
}

.login__forgot:hover {
  color: hsl(208, 92%, 54%); /* First color */
}

.login__button {
  width: 100%;
  display: inline-flex;
  justify-content: center;
  background-color: hsl(208, 92%, 54%); /* First color */
  color: #ffffff; /* White color */
  font-weight: 600; /* Semi-bold font */
  padding-block: 1.5rem;
  border-radius: 4rem;
  margin-block: 2rem;
  cursor: pointer;
  transition: background-color .4s, box-shadow .4s;
}

.login__button:hover {
  background-color: hsl(208, 88%, 50%); /* First color alternate */
  box-shadow: 0 8px 24px hsla(208, 92%, 32%, .3);
}

.login__social {
  margin-bottom: 2rem;
}

.login__social-title {
  text-align: center;
  font-size: .813rem; /* Small font size */
  font-weight: 600; /* Semi-bold font */
  color: hsl(220, 68%, 4%); /* Title color */
  margin-bottom: 1rem;
}

.login__social-img {
  width: 1rem;
}

.login__social-links {
  display: flex;
  justify-content: center;
  column-gap: 1.5rem;
}

.login__social-link {
  width: 32px;
  height: 32px;
  background-color: #ffffff; /* Body color (white) */
  box-shadow: 0 4px 8px hsla(0, 0%, 0%, .1);
  border-radius: .5rem;
  display: grid;
  place-items: center;
  transition: transform .4s;
}

.login__social-link:hover {
  transform: translateY(-.25rem);
}

.login__switch {
  text-align: center;
  font-size: .813rem; /* Small font size */
}

.login__switch button {
  background: none;
  color: hsl(208, 92%, 54%); /* First color */
  font-size: .813rem; /* Small font size */
  font-weight: 600; /* Semi-bold font */
  cursor: pointer;
  transition: color .4s;
}

.login__switch button:hover {
  color: hsl(208, 88%, 50%); /* First color alternate */
}

.login__access, 
.login__register {
  position: absolute;
  left: 0;
  right: 0;
  width: 100%;
  transition: transform .4s, opacity .4s .1s;
}

.login__register {
  transform: translateX(15rem);
  opacity: 0;
  pointer-events: none;
}

/* Input focus move up label */
.login__input:focus ~ .login__label {
  transform: translateY(-12px);
  font-size: .688rem; /* Tiny font size */
}

.login__input:focus {
  padding-block: 2rem 1rem;
}

/* Input focus sticky top label */
.login__input:not(:placeholder-shown).login__input:not(:focus) ~ .login__label {
  transform: translateY(-12px);
  font-size: .688rem; /* Tiny font size */
}

.login__input:not(:placeholder-shown).login__input:not(:focus) {
  padding-block: 2rem 1rem;
}

/* Input focus color */
.login__input:focus {
  border-color: hsl(208, 92%, 54%); /* First color */
}

.login__input:focus ~ .login__label,
.login__input:focus ~ .login__icon {
  color: hsl(208, 92%, 54%); /* First color */
}

/* Show hide login & create account */
.active .login__access {
  transform: translateX(15rem);
  opacity: 0;
  pointer-events: none;
}

.active .login__register {
  transform: translateX(0);
  opacity: 1;
  pointer-events: initial;
}

/*=============== BREAKPOINTS ===============*/
/* For small devices */
@media screen and (max-width: 320px) {
  .container {
    margin-inline: 1rem;
  }
}

/* For medium devices */
@media screen and (min-width: 540px) {
  .login__area {
    width: 380px;
    margin-inline: auto;
  }
}

@media screen and (min-width: 350px) and (max-height: 600px) {
  .login {
    height: 760px;
  }
}

/* For large devices */
@media screen and (min-width: 1150px) {
  .container {
    margin-inline: auto;
  }

  .login__area {
    width: 460px;
    margin: initial;
  }
  .login__access, 
  .login__register {
    width: 600px;
  }
  .login__title {
    text-align: initial;
    margin-bottom: 3rem;
  }
  .login__button {
    margin-bottom: 3rem;
  }
  .login__group {
    grid-template-columns: repeat(2, 1fr);
  }
  .login__blob {
    display: block;
    height: 100vh;
    position: absolute;
    top: 0;
    right: 0;
  }
  .login__img {
    transform: translate(0rem, 0rem);
  }
}
.fade-enter-active, .fade-leave-active {
  transition: opacity 0.5s ease;
}
.fade-enter, .fade-leave-to {
  opacity: 0;
}


</style>
